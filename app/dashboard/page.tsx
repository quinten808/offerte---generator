"use client";
import Link from "next/link";
import { useCustomers } from "@/app/hooks/use-customers";
import { useQuotes } from "@/app/hooks/use-quotes";
export default function DashboardPage() { const customers = useCustomers(); const quotes = useQuotes(); const cards = [{ label: "Klanten", value: customers.length, description: "klanten in uw overzicht" }, { label: "Offertes", value: quotes.length, description: "opgeslagen offertes" }, { label: "Concepten", value: quotes.filter((quote) => quote.status === "Concept").length, description: "klaar om af te ronden" }]; return <><header className="flex flex-col gap-5 border-b border-slate-200 pb-7 sm:flex-row sm:items-end sm:justify-between"><div><p className="mb-2 text-sm font-medium text-blue-700">Dashboard</p><h1 className="text-3xl font-semibold tracking-tight text-slate-950 sm:text-4xl">Goedemorgen</h1><p className="mt-3 max-w-xl text-sm leading-6 text-slate-600 sm:text-base">Hier vindt u in één oogopslag de voortgang van uw klanten en offertes.</p></div><Link className="inline-flex items-center justify-center rounded-lg bg-blue-700 px-4 py-2.5 text-sm font-semibold text-white shadow-sm hover:bg-blue-800" href="/offertes/nieuw">Nieuwe offerte</Link></header><section aria-label="Overzicht" className="mt-8 grid gap-4 sm:grid-cols-3">{cards.map((card) => <article className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm" key={card.label}><p className="text-sm font-medium text-slate-600">{card.label}</p><p className="mt-3 text-3xl font-semibold tracking-tight text-slate-950">{card.value}</p><p className="mt-2 text-sm text-slate-500">{card.description}</p></article>)}</section></>; }
